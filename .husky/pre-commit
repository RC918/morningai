#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

set -e

# Run lint-staged for both apps
echo "Running lint-staged for frontend-dashboard..."
cd handoff/20250928/40_App/frontend-dashboard && pnpm exec lint-staged

echo "Running lint-staged for owner-console..."
cd ../owner-console && pnpm exec lint-staged

echo "✅ Pre-commit checks passed!"
