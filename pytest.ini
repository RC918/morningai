[pytest]
addopts = -q
# 只找我們 repo 根與預設 tests；不要掃 handoff 內部外部套件測試
testpaths =
  .
norecursedirs =
  handoff/20250928/60_Design/testing
  handoff/20250928/99_Original_Bundle
markers =
  asyncio: async tests
  e2e: end-to-end tests
  slow: slow tests
