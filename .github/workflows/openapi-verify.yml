name: openapi-verify
on:
  workflow_dispatch:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  openapi-placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: |
          pip install check-openapi-spec || true
          echo "openapi spec validation (placeholder)"
