{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "node -v && pnpm -v && pnpm -w list -r --depth -1 && pnpm -w --filter @morningai/shared-ui build && pnpm -w --filter owner-console build",
  "installCommand": "corepack prepare pnpm@9.15.1 --activate && pnpm -w install",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
