# Test Task 6 for Planner Distribution Verification

## Overview

In the MorningAI platform, ensuring that planner distribution is correctly verified is crucial for maintaining system integrity and performance. This process involves checking that tasks are evenly distributed among available workers and that each task is assigned to the most appropriate worker based on current load and capabilities. This FAQ aims to guide developers through the verification process of task distribution within the planner module, a key component of MorningAI's autonomous agent system.

## How to Verify Planner Distribution

Verifying planner distribution involves several steps to ensure tasks are allocated efficiently across the system's workers. Here's a comprehensive guide:

### Step 1: Understanding the Distribution Logic

Before diving into verification, it's important to understand how MorningAI's planner distribution logic works. The system uses a combination of round-robin and load-balancing algorithms to distribute tasks. This ensures not only an even distribution of tasks but also considers the current load and capabilities of each worker.

### Step 2: Setup for Verification

Ensure your environment is correctly set up:

- Clone the repository: `git clone https://github.com/RC918/morningai.git`
- Navigate to the project directory: `cd morningai`
- Install dependencies: `pip install -r requirements.txt`

### Step 3: Triggering Task Distribution

To start verifying task distribution, you need to trigger tasks manually or via an automated test script. Here's a simple example using Python:

```python
from morningai.planner import distribute_task

# Example task data
task_data = {"task_id": "12345", "priority": "high"}

# Distribute task
distribute_task(task_data)
```

Replace `morningai.planner` with the correct path if different in your setup.

### Step 4: Monitoring Task Allocation

After triggering tasks, monitor their allocation across workers. This can be done by examining the logs generated by the system:

```shell
tail -f logs/morningai.log
```

Look for entries related to task distribution, which should provide details on which worker picked up each task.

### Related Documentation Links

- [MorningAI Task Orchestration Guide](/docs/task_orchestration.md)
- [Understanding MorningAI's Load Balancing](/docs/load_balancing.md)

## Common Troubleshooting Tips

- **Tasks Not Being Distributed**: Ensure Redis Queue (RQ) is running and accessible. Check RQ dashboard for any stuck jobs.
  
  ```shell
  rq info --url redis://localhost:6379
  ```

- **Uneven Task Distribution**: Verify that all workers are online and registered in Redis Queue. Sometimes, a worker might have crashed or failed to register correctly.

- **Performance Issues**: If certain tasks are taking significantly longer to complete, causing bottlenecks, review the task allocation logic. Consider implementing priority queues or adjusting the load balancing algorithm.

For more detailed troubleshooting related to specific errors or issues encountered during planner distribution verification, refer to the project's issue tracker or consult the technical documentation available in the repository.

---
Generated by MorningAI Orchestrator using GPT-4

---

**Metadata**:
- Task: Test task 6 for planner distribution verification
- Trace ID: `planner-dist-check-6`
- Generated by: MorningAI Orchestrator using gpt-4-turbo-preview
- Repository: RC918/morningai
